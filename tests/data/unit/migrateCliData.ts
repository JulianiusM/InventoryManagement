/**
 * Test data for the CLI migration tool.
 */

export const validCommands = ['run', 'revert', 'show'];

export const helpFlags = ['--help', '-h'];

export const invalidCommands = ['invalid', 'migrate', 'drop', ''];
